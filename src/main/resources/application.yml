spring:
  application:
    name: batch-server
  profiles:
    active: none
  config:
    import: optional:configserver:${CONFIG_SERVER_URL}

  batch:
    job:
      name: beforeEventStartJob

  cloud:
    config:
      profile: ${PROFILE}

encrypt:
  key: ${ENCRYPT_KEY}


